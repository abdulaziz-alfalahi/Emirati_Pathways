# =============================================================================
# LiveKit Interview Insight & Compliance — Docker Compose
#
# Services:
#   livekit-server  — WebRTC SFU (:7880)
#   livekit-egress  — Headless Chrome recorder → S3
#   minio           — S3-compatible encrypted object storage (:9000/:9001)
#   agent-worker    — AI Transcription Python Bridge
# =============================================================================

services:

  # ─── LiveKit SFU Server ──────────────────────────────────────────────
  livekit-server:
    image: livekit/livekit-server:latest
    command: --config /etc/livekit.yaml
    ports:
      - "7880:7880"   # WebSocket + HTTP
      - "7881:7881"   # TCP TURN
      - "7882:7882/udp"  # UDP (WebRTC/ICE)
    volumes:
      - ./livekit.yaml:/etc/livekit.yaml:ro
    environment:
      - "LIVEKIT_KEYS=${LIVEKIT_API_KEY}: ${LIVEKIT_API_SECRET}"
    restart: unless-stopped
    # LiveKit had no healthcheck, so a crash loop was invisible: a bad config
    # makes it exit 0 during startup, and `unless-stopped` then restarts it
    # every ~60s forever with no unhealthy state and nothing to alert on. That
    # ran to 2,379 restarts before anyone noticed, with video interviews down
    # the whole time. This asserts the signalling HTTP server is actually
    # serving, not merely that the process exists.
    # -Y off: BusyBox wget honours http_proxy but ignores no_proxy, so if proxy
    # build args are ever added to this image the check would be answered by the
    # corporate proxy with a 503 (this exact bug hit emirati_frontend).
    healthcheck:
      test: ["CMD-SHELL", "wget -Y off -qO- http://localhost:7880/ >/dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - emirati_net

  # ─── LiveKit Egress (Recording) ─────────────────────────────────────
  livekit-egress:
    image: livekit/egress:latest
    environment:
      # LiveKit connection
      - LIVEKIT_URL=ws://livekit-server:7880
      - LIVEKIT_API_KEY=${LIVEKIT_API_KEY}
      - LIVEKIT_API_SECRET=${LIVEKIT_API_SECRET}
      # S3 upload (MinIO)
      - AWS_ACCESS_KEY_ID=${MINIO_ROOT_USER}
      - AWS_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD}
      - AWS_DEFAULT_REGION=us-east-1
    volumes:
      - egress-tmp:/tmp/egress
    depends_on:
      - livekit-server
      - minio
    restart: unless-stopped
    networks:
      - emirati_net
    cap_add:
      - SYS_ADMIN   # Required for headless Chrome

  # ─── LiveKit Agent Worker (AI Transcription) ────────────────────────
  agent-worker:
    build:
      context: .
      dockerfile: Dockerfile.agent
    environment:
      - LIVEKIT_URL=ws://livekit-server:7880
      - LIVEKIT_API_KEY=${LIVEKIT_API_KEY}
      - LIVEKIT_API_SECRET=${LIVEKIT_API_SECRET}
      - GRANITE_SPEECH_URL=http://10.228.145.194:8001/v1
    depends_on:
      - livekit-server
    restart: unless-stopped
    networks:
      - emirati_net

  # ─── MinIO (S3-Compatible Object Storage) ───────────────────────────
  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"   # S3 API
      - "9001:9001"   # MinIO Console UI
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
      # AES-256 Server-Side Encryption (SSE-S3)
      - MINIO_KMS_AUTO_ENCRYPTION=on
      - MINIO_KMS_SECRET_KEY=${MINIO_KMS_SECRET_KEY}
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - emirati_net

  # ─── MinIO Bucket Init ──────────────────────────────────────────────
  minio-init:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 $$MINIO_ROOT_USER $$MINIO_ROOT_PASSWORD;
      mc mb --ignore-existing local/interview-recordings;
      mc anonymous set none local/interview-recordings;
      mc encrypt set sse-s3 local/interview-recordings;
      echo 'Bucket ready with SSE-S3 encryption';
      "
    networks:
      - emirati_net

volumes:
  minio-data:
    driver: local
  egress-tmp:
    driver: local

networks:
  emirati_net:
    external: true
