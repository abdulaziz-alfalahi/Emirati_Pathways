import React from 'react';

// Skip navigation component - now empty to remove skip links
// This component is kept to maintain compatibility with existing imports
export const SkipNavigation: React.FC = () => {
  return null;
};

