[pytest]
testpaths = backend/tests
markers =
    smoke: Quick smoke tests that verify basic imports and setup
    unit: Unit tests for individual functions/methods
    integration: Integration tests requiring database or external services
